import type{SpeechToTextProvider,STTConfig}from'../shared/types';import{WebSpeechProvider}from'./web-speech-provider';import{StreamingWebSocketProvider}from'./streaming-websocket-provider';export function createSTTProvider(config:STTConfig):SpeechToTextProvider{if(config.provider==='web-speech')return new WebSpeechProvider();if(config.provider==='streaming-websocket')return new StreamingWebSocketProvider();throw new Error('Unknown speech-to-text provider.');}
