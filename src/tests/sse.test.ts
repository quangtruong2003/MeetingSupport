import{describe,expect,it}from'vitest';import{parseSSE}from'../ai/sse';function stream(parts:string[]){return new ReadableStream<Uint8Array>({start(controller){for(const part of parts)controller.enqueue(new TextEncoder().encode(part));controller.close()}})}describe('parseSSE',()=>{it('parses frames split across chunks',async()=>{const values=[];for await(const value of parseSSE(stream(['data: {"choices":[{"delta":{"content":"Hel','lo"}}]}\n\n','data: [DONE]\n\n'])))values.push(value);expect(values).toHaveLength(1)});it('rejects malformed JSON',async()=>{await expect(async()=>{for await(const _ of parseSSE(stream(['data: nope\n\n'])))void _}).rejects.toThrow()})});
