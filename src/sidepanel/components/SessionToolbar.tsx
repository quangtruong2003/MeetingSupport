import type{SessionStatus}from'../../shared/types';export function SessionToolbar({status,tabTitle,onStart,onPause,onResume,onStop,onClear}:{status:SessionStatus;tabTitle:string;onStart():void;onPause():void;onResume():void;onStop():void;onClear():void}){return <section className="card toolbar"><div><label>Tab cuộc họp</label><div className="tab-choice"><span className="tab-dot"/>{tabTitle||'Không tìm thấy tab đang hoạt động'}</div></div><div className="actions">{['idle','error','disconnected'].includes(status)?<button onClick={onStart} disabled={!tabTitle}>Bắt đầu hỗ trợ</button>:null}{status==='listening'?<button className="secondary" onClick={onPause}>Tạm dừng</button>:null}{status==='paused'?<button onClick={onResume}>Tiếp tục</button>:null}{status!=='idle'?<button className="danger ghost" onClick={onStop}>Dừng</button>:null}<button className="ghost" onClick={onClear}>Xóa transcript</button></div></section>}
