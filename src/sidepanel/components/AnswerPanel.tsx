export function AnswerPanel({question,text,status,error,onGenerate,onCancel}:{question:string;text:string;status:string;error:string;onGenerate():void;onCancel():void}){return <section className="answer-card"><div className="question"><span>Câu hỏi gần nhất</span><strong>{question||'Chưa phát hiện câu hỏi'}</strong></div><div className="answer-head"><h2>Gợi ý trả lời</h2>{text&&<button className="icon-button" onClick={()=>void navigator.clipboard.writeText(text)}>Sao chép</button>}</div><div className={`answer ${status==='generating'?'streaming':''}`}>{text||<span>AI chỉ tạo câu trả lời khi bạn chủ động yêu cầu.</span>}</div>{error&&<p className="error">{error}</p>}<div className="actions">{status==='generating'?<button className="danger" onClick={onCancel}>Dừng tạo</button>:<button onClick={onGenerate}>Tạo câu trả lời</button>}</div></section>}
