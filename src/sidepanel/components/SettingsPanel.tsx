import{useState}from'react';import type{StoredSettings}from'../../shared/types';export function SettingsPanel({settings,onSave,onClose}:{settings:StoredSettings;onSave(value:StoredSettings):Promise<void>;onClose():void}){const[value,setValue]=useState(settings);const field=(key:keyof StoredSettings,next:string)=>setValue(current=>({...current,[key]:next}));return <div className="settings-overlay"><section className="settings card"><div className="section-title"><h2>Cài đặt</h2><button className="ghost" onClick={onClose}>Đóng</button></div><label>Ngôn ngữ<select value={value.language} onChange={e=>field('language',e.target.value)}><option value="vi-VN">Tiếng Việt</option><option value="en-US">English</option></select></label><label>STT provider<select value={value.sttProvider} onChange={e=>field('sttProvider',e.target.value as StoredSettings['sttProvider'])}><option value="web-speech">Web Speech fallback</option><option value="streaming-websocket">Streaming WebSocket</option></select></label>{value.sttProvider==='streaming-websocket'&&<><label>STT WebSocket endpoint<input value={value.sttEndpoint} onChange={e=>field('sttEndpoint',e.target.value)}/></label><label>STT API key<input type="password" value={value.sttApiKey} onChange={e=>field('sttApiKey',e.target.value)}/></label></>}<p className="notice">Web Speech là fallback thử nghiệm, có thể không nhận audio tab. Production nên cấu hình provider streaming.</p><label>OpenRouter API key<input type="password" value={value.openRouterApiKey} onChange={e=>field('openRouterApiKey',e.target.value)}/></label><label>Model<input value={value.openRouterModel} onChange={e=>field('openRouterModel',e.target.value)}/></label><label>Hồ sơ người dùng<textarea value={value.userProfile} onChange={e=>field('userProfile',e.target.value)}/></label><label>System prompt<textarea rows={4} value={value.systemPrompt} onChange={e=>field('systemPrompt',e.target.value)}/></label><button onClick={()=>void onSave(value)}>Lưu cài đặt</button><p className="notice">API key lưu trong Chrome profile. Production nhiều người dùng cần backend proxy.</p></section></div>}
