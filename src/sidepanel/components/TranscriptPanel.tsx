import type{TranscriptSegment as Segment}from'../../shared/types';export function TranscriptPanel({segments,partial}:{segments:Segment[];partial:Segment|null}){return <section className="card transcript-card"><div className="section-title"><h2>Transcript trực tiếp</h2><span>{segments.length} đoạn</span></div><div className="transcript" aria-label="Transcript cuộc họp">{segments.length===0&&!partial?<div className="empty-inline"><b>Chưa có nội dung</b><span>Transcript sẽ xuất hiện khi STT nhận được giọng nói.</span></div>:segments.slice(-80).map(segment=><article key={segment.id}><time>{new Date(segment.timestamp).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</time><p>{segment.text}</p></article>)}{partial&&<article className="partial"><time>•••</time><p>{partial.text}</p></article>}</div></section>}
