import type{SessionStatus}from'../../shared/types';export function StatusBadge({status}:{status:SessionStatus}){const labels:Record<SessionStatus,string>={idle:'Chưa bắt đầu',connecting:'Đang kết nối',listening:'Đang nghe',paused:'Tạm dừng',disconnected:'Mất kết nối',stopping:'Đang dừng',error:'Có lỗi'};return <span className={`status ${status}`}>{status==='listening'&&<i/>}{labels[status]}</span>}
