import{z}from'zod';import type{StoredSettings}from'../shared/types';export const defaultSettings:StoredSettings={language:'vi-VN',sttProvider:'openrouter',sttEndpoint:'',sttApiKey:'',openRouterApiKey:'',openRouterModel:'openai/gpt-4o-mini',openRouterSttModel:'',userProfile:'',systemPrompt:'You are a concise meeting assistant. Answer using only provided context. State uncertainty clearly.'};const schema=z.object({language:z.string().min(2),sttProvider:z.enum(['openrouter','web-speech','streaming-websocket']),sttEndpoint:z.string(),sttApiKey:z.string(),openRouterApiKey:z.string(),openRouterModel:z.string().min(1),openRouterSttModel:z.string(),userProfile:z.string(),systemPrompt:z.string().min(1)});export async function loadSettings():Promise<StoredSettings>{const raw=await chrome.storage.local.get('settings');const result=schema.safeParse({...defaultSettings,...(raw.settings as Partial<StoredSettings>|undefined)});return result.success?result.data:defaultSettings}export async function saveSettings(settings:StoredSettings){await chrome.storage.local.set({settings:schema.parse(settings)})}export function validateOpenRouterKey(key:string){return key.trim().length>10}
